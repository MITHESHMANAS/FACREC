MONGODB_URI=mongodb://127.0.0.1:27017/facrec
JWT_SECRET=change-this-to-a-long-random-string
PORT=5000

# Optional - only needed if "python" isn't on PATH or you want to pin
# a specific interpreter (e.g. a venv). Defaults to "python".
PYTHON_PATH=python
